"use client";

import React from "react";
import ContainerWrapper from "@/components/ContainerWrapper";
import { Box, Flex, Text } from "@chakra-ui/react";

const Sweep: React.FC = () => {
  return (
    <ContainerWrapper>
      <Flex pt={"6rem"}>
        <Text>Sweep Page is here</Text>
      </Flex>
    </ContainerWrapper>
  );
};

export default Sweep;
